{{#each success}}
    <div class="container-fluid success notifications">
        <div class="row">
            <div class="col-12">
                    <p>{{this}}</p>
            </div>
        </div>
    </div>
{{/each}}

{{#each error}}
    <div class="container-fluid error notifications">
        <div class="row">
            <div class="col-12">
                    <p>{{this}}</p>
            </div>
        </div>
    </div>
{{/each}}