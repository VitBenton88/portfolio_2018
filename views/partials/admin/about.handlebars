{{#each portfolio}}
    <form action="/updateabout" method="POST">
        <textarea name="about" id="aboutEditor">{{about}}</textarea>
        <input name="_id" type="hidden" value="{{id}}">
        <button type="submit" class="btn btn-outline-success">Save</button>
    </form>
    <br />
{{/each}}